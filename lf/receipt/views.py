from django.shortcuts import render
import os, json

# Create your views here.
# ocr 결과물을 json으로 받는 함수
def ocr(request):
    return None

# json파일에서 날짜, 장소, 지불 가격 3가지 정보 추출
def to_json(request):
    return None